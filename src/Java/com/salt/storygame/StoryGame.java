package com.salt.storygame;

import com.salt.storygame.Item.IGearHandler;
import com.salt.storygame.Player.Player;
import com.salt.storygame.SaveManager.SaveManager;

import java.io.File;

public class StoryGame {
    /*
    public static final String GAME_ID = "SGM";
    public static final String TITLE = "Story Game";

    public static byte chapter = 1;
    public static byte scene = 1;
    public static void main(String[] args) {
        File dir = new File("src/Resources/com/salt/storygame/Saves");
        File[] fileList = dir.listFiles((dir1, name) -> name.endsWith(".ser"));
        if (fileList.length > 0) {
            _init_();
        }
        _runtime_();
        SaveManager.save();
    }

    public static void _init_() {
        SaveManager.get();
    }

    public static void _runtime_() {
        welcome();
        SceneManager.load();
    }
     */
}
